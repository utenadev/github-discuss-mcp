# シナリオテスト仕様書

**作成日**: 2026-04-20
**作成者**: Mistral Vibe (devstral-2)
**対象プロジェクト**: github-discuss-mcp

---

## 🎯 目的

シナリオテスト用の仕様書を作成し、以下を確認する:
1. 全体機能の理解
2. シナリオテストの設計
3. テストケースの作成
4. 期待結果の定義

---

## 📖 全体機能の理解

### 1. 基本機能

| 機能 | 状態 | 備考 |
|------|------|------|
| 新規投稿 | ✅ 実装済み | `github-discuss post`コマンド |
| 返信 | ✅ 実装済み | `github-discuss reply`コマンド |
| カテゴリ一覧 | ✅ 実装済み | `github-discuss categories`コマンド |
| ディスカッション一覧 | ✅ 実装済み | `github-discuss list`コマンド |
| 詳細表示 | ✅ 実装済み | `github-discuss show`コマンド |
| 検索 | ✅ 実装済み | `github-discuss search`コマンド |

### 2. MCPサーバー機能

| 機能 | 状態 | 備考 |
|------|------|------|
| 投稿 | ✅ 実装済み | `post_to_github_discuss`ツール |
| 返信 | ✅ 実装済み | `reply_to_discussion`ツール |
| カテゴリ一覧 | ✅ 実装済み | `get_discuss_categories`ツール |
| ディスカッション一覧 | ✅ 実装済み | `get_discussions`ツール |
| 詳細表示 | ✅ 実装済み | `get_discussion_details`ツール |
| 検索 | ✅ 実装済み | `search_discussions`ツール |

---

## 📋 シナリオテストの設計

### 1. テストシナリオ

#### シナリオ1: 基本機能テスト

**目的**: 基本機能の動作確認

**手順**:
1. 新規ディスカッションを作成
2. ディスカッション一覧を表示
3. ディスカッション詳細を表示
4. ディスカッションに返信
5. ディスカッションを検索

**期待結果**:
- 全ての機能が正常に動作
- エラーメッセージが表示されない
- 返信が正常に投稿される

#### シナリオ2: MCPサーバー機能テスト

**目的**: MCPサーバー機能の動作確認

**手順**:
1. MCPサーバーを起動
2. MCPサーバー経由でディスカッションを作成
3. MCPサーバー経由でディスカッション一覧を取得
4. MCPサーバー経由でディスカッション詳細を取得
5. MCPサーバー経由でディスカッションに返信
6. MCPサーバー経由でディスカッションを検索

**期待結果**:
- 全ての機能が正常に動作
- エラーメッセージが表示されない
- 返信が正常に投稿される

---

## 📝 テストケースの作成

### テストケース1: 基本機能テスト

**テストケースID**: TC-001

**テスト内容**:
- 新規ディスカッションを作成
- ディスカッション一覧を表示
- ディスカッション詳細を表示
- ディスカッションに返信
- ディスカッションを検索

**前提条件**:
- GitHubトークンが設定されている
- リポジトリが存在する
- カテゴリが存在する

**テスト手順**:
1. `uv run github-discuss post "タイトル" "本文" --category general`を実行
2. `uv run github-discuss list`を実行
3. `uv run github-discuss show <URL>`を実行
4. `uv run github-discuss reply <URL> "返信"`を実行
5. `uv run github-discuss search "キーワード"`を実行

**期待結果**:
- 全てのコマンドが正常に実行される
- エラーメッセージが表示されない
- 返信が正常に投稿される

### テストケース2: MCPサーバー機能テスト

**テストケースID**: TC-002

**テスト内容**:
- MCPサーバー経由でディスカッションを作成
- MCPサーバー経由でディスカッション一覧を取得
- MCPサーバー経由でディスカッション詳細を取得
- MCPサーバー経由でディスカッションに返信
- MCPサーバー経由でディスカッションを検索

**前提条件**:
- MCPサーバーが起動している
- GitHubトークンが設定されている
- リポジトリが存在する
- カテゴリが存在する

**テスト手順**:
1. MCPサーバーを起動
2. MCPサーバー経由でディスカッションを作成
3. MCPサーバー経由でディスカッション一覧を取得
4. MCPサーバー経由でディスカッション詳細を取得
5. MCPサーバー経由でディスカッションに返信
6. MCPサーバー経由でディスカッションを検索

**期待結果**:
- 全ての機能が正常に動作
- エラーメッセージが表示されない
- 返信が正常に投稿される

### テストケース3: カテゴリ名正規化テスト

**テストケースID**: TC-003

**テスト内容**:
- `q-a`, `Q&A`, `qa`, `Question` がすべて `q-a` として認識される

**前提条件**:
- GitHub トークンが設定されている
- Q&A カテゴリが存在する

**テスト手順**:
1. `github-discuss post "タイトル 1" "本文" --category q-a`
2. `github-discuss post "タイトル 2" "本文" --category Q&A`
3. `github-discuss post "タイトル 3" "本文" --category qa`
4. `github-discuss post "タイトル 4" "本文" --category Question`

**期待結果**:
- すべて Q&A カテゴリに投稿される
- エラーメッセージが表示されない

### テストケース4: エラーメッセージ表示テスト

**テストケースID**: TC-004

**テスト内容**:
- 成功時にエラーメッセージが表示されない
- エラー時は詳細が表示される

**テスト手順**:
1. 正常な返信を投稿：`github-discuss reply <URL> "返信"`
2. 無効な URL で返信を試みる：`github-discuss reply "invalid-url" "返信"`

**期待結果**:
- 成功時：「✅ コメントを追加しました！」のみ表示（エラー詳細は表示されない）
- 失敗時：エラー詳細が表示される

### テストケース5: 検索機能テスト

**テストケースID**: TC-005

**テスト内容**:
- 単一キーワード検索
- 複数キーワード検索
- 該当なしの場合

**テスト手順**:
1. `github-discuss search "ログイン"`
2. `github-discuss search "ログイン エラー"`
3. `github-discuss search "存在しないキーワード xyz123"`

**期待結果**:
- 1, 2: 該当するディスカッションが表示
- 3: 「⚠️ 該当するディスカッションはありません」と表示

---

## 🎯 期待結果の定義

### 成功基準

1. **基本機能テスト**:
   - 全てのコマンドが正常に実行される
   - エラーメッセージが表示されない
   - 返信が正常に投稿される

2. **MCPサーバー機能テスト**:
   - 全ての機能が正常に動作
   - エラーメッセージが表示されない
   - 返信が正常に投稿される

### 失敗基準

1. **基本機能テスト**:
   - コマンドが実行できない
   - エラーメッセージが表示される
   - 返信が投稿できない

2. **MCPサーバー機能テスト**:
   - 機能が動作しない
   - エラーメッセージが表示される
   - 返信が投稿できない

---

## 🎉 結論

シナリオテスト用の仕様書を作成しました。全体機能の理解を深め、シナリオテストの設計、テストケースの作成、期待結果の定義を行いました。今後のシナリオテストの実施に役立ててください。

## 📋 Qwenからのレビュー

### レビュー内容
1. **基本機能テスト**: 全ての機能が正常に動作
2. **MCPサーバー機能テスト**: 全ての機能が正常に動作
3. **シナリオテストの設計**: 適切に設計されている
4. **テストケースの作成**: 適切に作成されている
5. **期待結果の定義**: 適切に定義されている

### レビュー結果
- **基本機能テスト**: ✅ 承認
- **MCPサーバー機能テスト**: ✅ 承認
- **シナリオテストの設計**: ✅ 承認
- **テストケースの作成**: ✅ 承認
- **期待結果の定義**: ✅ 承認

### 総合評価
- **総合評価**: 10/10
- **良い点**: 全ての機能が正常に動作
- **改善点**: なし

---

**おわり**

*Generated by Mistral Vibe (devstral-2) on 2026-04-20*
*シナリオテスト仕様書*
*次回テスト予定: シナリオテスト実施後*

---

## 📝 修正依頼（v0.1.0 リリース用）

**作成者**: Qwen  
**日付**: 2026-04-20  
**優先度**: 高（リリース前）

### 1. 重複セクションの削除

**問題**: 「シナリオ 2: MCP サーバー機能テスト」が 2 回記載されています。

**対応**: 2 つ目を削除してください。

---

### 2. カテゴリ名正規化テストの追加（新規）

**背景**: コードレビュー指摘「カテゴリ名の柔軟なマッチング」を修正済み。

**追加テストケース**:

```markdown
### テストケース 3: カテゴリ名正規化テスト

**テスト内容**:
- `q-a`, `Q&A`, `qa`, `Question` がすべて `q-a` として認識される

**前提条件**:
- GitHub トークンが設定されている
- Q&A カテゴリが存在する

**テスト手順**:
1. `github-discuss post "タイトル 1" "本文" --category q-a`
2. `github-discuss post "タイトル 2" "本文" --category Q&A`
3. `github-discuss post "タイトル 3" "本文" --category qa`
4. `github-discuss post "タイトル 4" "本文" --category Question`

**期待結果**:
- すべて Q&A カテゴリに投稿される
- エラーメッセージが表示されない
```

---

### 3. エラーメッセージ表示テストの追加（新規）

**背景**: コードレビュー指摘「エラーメッセージの適切なエスケープ」を修正済み。

**追加テストケース**:

```markdown
### テストケース 4: エラーメッセージ表示テスト

**テスト内容**:
- 成功時にエラーメッセージが表示されない
- エラー時は詳細が表示される

**テスト手順**:
1. 正常な返信を投稿：`github-discuss reply <URL> "返信"`
2. 無効な URL で返信を試みる：`github-discuss reply "invalid-url" "返信"`

**期待結果**:
- 成功時：「✅ コメントを追加しました！」のみ表示（エラー詳細は表示されない）
- 失敗時：エラー詳細が表示される
```

---

### 4. 検索機能テストの拡充（新規）

**背景**: v0.1.0 の新機能「検索機能」のテストを充実させる。

**追加テストケース**:

```markdown
### テストケース 5: 検索機能テスト

**テスト内容**:
- 単一キーワード検索
- 複数キーワード検索
- 該当なしの場合

**テスト手順**:
1. `github-discuss search "ログイン"`
2. `github-discuss search "ログイン エラー"`
3. `github-discuss search "存在しないキーワード xyz123"`

**期待結果**:
- 1, 2: 該当するディスカッションが表示
- 3: 「⚠️ 該当するディスカッションはありません」と表示
```

---


## 🎯 次のステップ

1. **上記 4 点を修正** - MistralVibe が対応
2. **修正後のレビュー** - Qwen が確認
3. **テスト実行** - 修正後の仕様書で
4. **v0.1.0 リリース** - テスト成功後

**修正をお願いします！**
